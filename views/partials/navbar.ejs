<nav id="navbar" class="fixed top-0 left-0 w-full z-50 transition-all duration-300">
  <div class="max-w-7xl mx-auto flex items-center justify-between px-6 py-4">
    <a href="/" class="flex items-center gap-2 font-extrabold text-lg brand-gradient-text flex-shrink-0">
      <span class="logo-dot"></span> SMARTLOAN AI
    </a>

    <!-- Desktop nav -->
    <div class="hidden md:flex items-center gap-5 text-sm">
      <% if (user && user.role !== 'admin') { %>
        <a href="/loan/dashboard" class="hover:text-cyan-400 transition">Dashboard</a>
        <a href="/loan/history" class="hover:text-cyan-400 transition">History</a>
        <a href="/loan/calculator" class="hover:text-cyan-400 transition">EMI Calculator</a>
        <a href="/loan/profile" class="hover:text-cyan-400 transition">Profile</a>
        <a href="/loan/settings" class="hover:text-cyan-400 transition">Settings</a>
        <a href="/loan/activity" class="hover:text-cyan-400 transition">Activity</a>

        <div class="flex items-center gap-2 pl-3 ml-1 border-l border-white/10">
          <div class="relative">
            <button id="notif-bell" class="relative p-1.5 hover:text-cyan-400 transition rounded-full" aria-label="Notifications">
              🔔
              <span id="notif-badge" class="hidden absolute -top-1 -right-1 bg-red-500 text-white text-[10px] font-bold rounded-full w-4 h-4 flex items-center justify-center">0</span>
            </button>
            <div id="notif-dropdown" class="hidden absolute right-0 mt-3 w-80 glass-card rounded-2xl p-3 max-h-96 overflow-y-auto z-50">
              <div class="flex justify-between items-center px-2 py-1">
                <p class="text-xs font-semibold text-slate-400">Notifications</p>
                <a href="/loan/notifications/history" class="text-xs text-cyan-400 hover:underline">View All</a>
              </div>
              <div id="notif-list" class="space-y-1 mt-1"></div>
            </div>
          </div>

          <button id="theme-toggle-btn" class="theme-toggle-btn" aria-label="Toggle theme">
            <span class="theme-icon">🌙</span>
          </button>

          <div class="theme-toggle-btn" style="width:auto; padding:0 8px;">
            <select onchange="window.location.href='/loan/language/' + this.value" class="bg-transparent text-xs px-1 py-1.5 cursor-pointer border-0 outline-none" style="width:auto;" aria-label="Language">
              <option value="en" <%= lang === 'en' ? 'selected' : '' %>>🌐 EN</option>
              <option value="hi" <%= lang === 'hi' ? 'selected' : '' %>>🌐 हि</option>
              <option value="te" <%= lang === 'te' ? 'selected' : '' %>>🌐 తె</option>
            </select>
          </div>

          <a href="/auth/logout" class="hover:text-red-400 transition">Logout</a>
        </div>
      <% } else if (user && user.role === 'admin') { %>
        <a href="/admin/dashboard" class="hover:text-cyan-400 transition">Dashboard</a>
        <a href="/admin/users" class="hover:text-cyan-400 transition">Users</a>
        <a href="/admin/applications" class="hover:text-cyan-400 transition">Applications</a>
        <a href="/admin/model" class="hover:text-cyan-400 transition">ML Model</a>

        <div class="flex items-center gap-2 pl-3 ml-1 border-l border-white/10">
          <button id="theme-toggle-btn" class="theme-toggle-btn" aria-label="Toggle theme">
            <span class="theme-icon">🌙</span>
          </button>
          <a href="/auth/logout" class="hover:text-red-400 transition">Logout</a>
        </div>
      <% } else { %>
        <a href="/#home" class="hover:text-cyan-400 transition">Home</a>
        <a href="/#how" class="hover:text-cyan-400 transition">How It Works</a>
        <a href="/#features" class="hover:text-cyan-400 transition">Features</a>
        <a href="/#faq" class="hover:text-cyan-400 transition">FAQ</a>

        <div class="flex items-center gap-3 pl-3 ml-1 border-l border-white/10">
          <button id="theme-toggle-btn" class="theme-toggle-btn" aria-label="Toggle theme">
            <span class="theme-icon">🌙</span>
          </button>
          <a href="/auth/login" class="hover:text-cyan-400 transition">Login</a>
          <a href="/auth/register" class="btn-gradient px-5 py-2 rounded-lg text-white font-semibold">Get Started</a>
        </div>
      <% } %>
    </div>

    <!-- Mobile controls -->
    <div class="md:hidden flex items-center gap-2">
      <button id="theme-toggle-btn-mobile" class="theme-toggle-btn" aria-label="Toggle theme">
        <span class="theme-icon">🌙</span>
      </button>
      <button id="mobile-menu-btn" class="text-current p-2" aria-label="Toggle menu">
        <svg id="icon-open" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
        <svg id="icon-close" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile menu panel -->
  <div id="mobile-menu" class="md:hidden hidden flex-col gap-1 px-6 pb-4 text-sm glass-card mx-4 rounded-2xl">
    <% if (user && user.role !== 'admin') { %>
      <a href="/loan/dashboard" class="py-3 border-b border-white/5">Dashboard</a>
      <a href="/loan/history" class="py-3 border-b border-white/5">History</a>
      <a href="/loan/calculator" class="py-3 border-b border-white/5">EMI Calculator</a>
      <a href="/loan/profile" class="py-3 border-b border-white/5">Profile</a>
      <a href="/loan/settings" class="py-3 border-b border-white/5">Settings</a>
      <a href="/loan/activity" class="py-3 border-b border-white/5">Activity</a>
      <a href="/loan/notifications/history" class="py-3 border-b border-white/5">Notifications</a>
      <a href="/auth/logout" class="py-3 text-red-400">Logout</a>
    <% } else if (user && user.role === 'admin') { %>
      <a href="/admin/dashboard" class="py-3 border-b border-white/5">Dashboard</a>
      <a href="/admin/users" class="py-3 border-b border-white/5">Users</a>
      <a href="/admin/applications" class="py-3 border-b border-white/5">Applications</a>
      <a href="/admin/model" class="py-3 border-b border-white/5">ML Model</a>
      <a href="/auth/logout" class="py-3 text-red-400">Logout</a>
    <% } else { %>
      <a href="/#home" class="py-3 border-b border-white/5">Home</a>
      <a href="/#how" class="py-3 border-b border-white/5">How It Works</a>
      <a href="/#features" class="py-3 border-b border-white/5">Features</a>
      <a href="/#faq" class="py-3 border-b border-white/5">FAQ</a>
      <a href="/auth/login" class="py-3 border-b border-white/5">Login</a>
      <a href="/auth/register" class="btn-gradient block text-center mt-3 mb-2 py-2.5 rounded-lg">Get Started</a>
    <% } %>
  </div>
</nav>

<script>
  window.addEventListener('scroll', () => {
    const nav = document.getElementById('navbar');
    if (window.scrollY > 20) nav.classList.add('nav-scrolled');
    else nav.classList.remove('nav-scrolled');
  });

  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  const iconOpen = document.getElementById('icon-open');
  const iconClose = document.getElementById('icon-close');
  if (btn && menu) {
    btn.addEventListener('click', () => {
      menu.classList.toggle('hidden');
      menu.classList.toggle('flex');
      iconOpen.classList.toggle('hidden');
      iconClose.classList.toggle('hidden');
    });
  }

  // Mirror the mobile theme button to the main one so theme.js's existing listener covers both
  const mobileThemeBtn = document.getElementById('theme-toggle-btn-mobile');
  if (mobileThemeBtn) {
    mobileThemeBtn.addEventListener('click', () => {
      window.setSmartLoanTheme(window.getSmartLoanTheme() === 'dark' ? 'light' : 'dark');
    });
  }
</script>

<% if (user && user.role !== 'admin') { %>
<script>
  (function () {
    const bell = document.getElementById('notif-bell');
    const dropdown = document.getElementById('notif-dropdown');
    const badge = document.getElementById('notif-badge');
    const list = document.getElementById('notif-list');

    function iconFor(type) {
      if (type === 'success') return '<span class="text-emerald-400">✓</span>';
      if (type === 'warning') return '<span class="text-amber-400">⚠</span>';
      return '<span class="text-cyan-400">ℹ</span>';
    }
    function timeAgo(dateStr) {
      const diff = (Date.now() - new Date(dateStr)) / 1000;
      if (diff < 60) return 'just now';
      if (diff < 3600) return Math.floor(diff / 60) + 'm ago';
      if (diff < 86400) return Math.floor(diff / 3600) + 'h ago';
      return Math.floor(diff / 86400) + 'd ago';
    }
    async function loadNotifications() {
      try {
        const res = await fetch('/loan/notifications');
        const data = await res.json();
        if (data.unreadCount > 0) {
          badge.textContent = data.unreadCount > 9 ? '9+' : data.unreadCount;
          badge.classList.remove('hidden');
        } else { badge.classList.add('hidden'); }

        if (data.notifications.length === 0) {
          list.innerHTML = '<p class="text-slate-500 text-xs text-center py-6">No notifications yet</p>';
          return;
        }
        list.innerHTML = data.notifications.map(n => `
          <a href="${n.link || '#'}" class="block px-2 py-2.5 rounded-lg hover:bg-white/5 transition text-xs ${n.read ? 'opacity-60' : ''}">
            <div class="flex items-start gap-2">
              ${iconFor(n.type)}
              <div><p class="text-slate-200">${n.message}</p><p class="text-slate-500 mt-0.5">${timeAgo(n.createdAt)}</p></div>
            </div>
          </a>`).join('');
      } catch (e) {}
    }
    bell.addEventListener('click', async (e) => {
      e.stopPropagation();
      dropdown.classList.toggle('hidden');
      if (!dropdown.classList.contains('hidden')) {
        await loadNotifications();
        await fetch('/loan/notifications/read', { method: 'POST' });
        setTimeout(() => badge.classList.add('hidden'), 800);
      }
    });
    document.addEventListener('click', (e) => {
      if (!dropdown.contains(e.target) && e.target !== bell) dropdown.classList.add('hidden');
    });
    loadNotifications();
    setInterval(loadNotifications, 30000);
  })();
</script>
<% } %>